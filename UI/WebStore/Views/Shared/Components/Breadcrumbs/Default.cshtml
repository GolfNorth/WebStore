@functions
{
    bool IsMainPage()
    {
        return Equals("Home", ViewContext.RouteData.Values["Controller"]) && Equals("Index", ViewContext.RouteData.Values["Action"]);
    }
}

@if (!IsMainPage())
{
    <breadcrumb></breadcrumb>
}